// Replaced by editorial card surfaces in globals.scss. Kept as a passthrough
// stub so any stale import resolves.
export default function GlowCard({ children, className = "" }) {
  return <div className={className}>{children}</div>;
}
